const ROLES = require('../../constants/roles.constants');

const STATUS = {
  PENDING: 'pending',
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  SUSPENDED: 'suspended',
};

module.exports = {
  ROLES,
  STATUS,
};
